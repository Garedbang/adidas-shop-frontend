import React from 'react';
import Header from './Header';
import Products from './Products';

export default () => {
  return (
    <section>
      <Header />
      <Products />
    </section>
  );
};
